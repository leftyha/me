import { useLanguage } from "@/hooks/useLanguage";
import { availableLinks, profile } from "@/data/profile";
export function Contact() {
  const { t } = useLanguage();
  const primary = availableLinks[0];
  return <section id="contact" className="scene overflow-hidden daylight"><div className="mx-auto max-w-4xl px-4 text-center"><p className="kicker">{t.contact.kicker}</p><h2 className="mt-4 font-display text-3xl leading-tight sm:text-5xl">{t.contact.title}<br/><span className="ink-gradient-text">{t.contact.titleAccent}</span></h2><p className="mx-auto mt-5 max-w-2xl text-lg leading-relaxed text-ink-soft">{t.contact.copy}</p><div className="mt-9 flex flex-wrap items-center justify-center gap-3">{primary&&<a href={primary.href} target="_blank" rel="noreferrer noopener" className="inline-flex min-h-12 items-center rounded-full px-7 text-sm font-semibold text-primary-foreground shadow-[var(--shadow-elevated)] transition-transform hover:-translate-y-0.5" style={{background:"var(--gradient-ink)"}}>{t.contact.cta}</a>}{availableLinks.map((link)=><a key={link.id} href={link.href} target="_blank" rel="noreferrer noopener" className="inline-flex min-h-12 items-center rounded-full border border-line bg-card px-6 text-sm font-medium text-ink transition-colors hover:bg-secondary">{link.label}</a>)}</div><p className="mt-6 font-mono text-[0.65rem] uppercase tracking-[0.2em] text-ink-soft">{t.contact.availability}</p><footer className="mt-16 flex flex-col items-center gap-2 border-t border-line pt-6 text-xs text-ink-soft sm:flex-row sm:justify-between"><p>© {new Date().getFullYear()} {profile.name} — {profile.jobTitle}</p><p>{t.contact.footer}</p></footer></div></section>;
}
